import { ImageErrorDirectiveDirective } from './image-error-directive.directive';

describe('ImageErrorDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ImageErrorDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
